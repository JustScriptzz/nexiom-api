{
  "name": "nexiom-api",
  "version": "1.1.0",
  "private": true,
  "description": "Nexiom - a single AI inference endpoint backed by a chain of failover paths.",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.0"
  }
}
